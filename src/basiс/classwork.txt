метод, который принимает строку - название фигуры "треугольник", "круг", "прямоугольник"
согласно тому, какая фигура пришла, вызвать соответствующий метод для подсчета площади фигуры.
Использовать switch()
Типы фигур задать с помощью enum
первый метод в качестве аргумента принимает enum - тип фигуры
дополнительные параметры (размеры сторон) запросить непосредственно в каждом отдельном методе
для подсчета площади треугольника использовать формулу Герона:
S = sqrt(p * (p - a) * (p - b) * (p - c)), где sqrt - корень квадратный, p - полупериметр (периметр / 2).
для площади круга использовать статическую переменную класса Math

результат вывести в консоль с округлением до 2 знаков после запятой

напишите метод, который принимает строку-название фигуры, в этом методе при помощи if-else-if вызовете следующий метод, соответствующий фигуре.
В каждом спец методе запросите размеры  и посчитайте площадь

- написать enum который содержит фигуры
- заменить if-else-if на switch (enum)
- метод checkFigure(принимает enum)

написать while-loop в которой будет проходить проверка на возможность существования данной фигуры
