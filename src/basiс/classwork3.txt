написать метод calculatePerimeter
метод должен принимать в качестве аргумента/аргементов дробное число
и возвращать дробное число раное периметру фигуры.

периметр круга = Math.PI * radius
периметр треугольника = a + b + c
периметр прямоугольника = ( a + b ) * 2

размеры радиуса и сторон получить из аргументов